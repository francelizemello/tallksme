<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.talksme">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.CadastrarUsuarioActivity"></activity>
        <activity android:name=".ui.activity.FraseActivity" />
        <activity android:name=".ui.activity.CadastrarMinhaFraseActivity" />
        <activity android:name=".ui.activity.MinhasActivity" />
        <activity android:name=".ui.activity.RecenteActivity" />
        <activity android:name=".ui.activity.CatalagoActivity" />
        <activity android:name=".ui.activity.SplashActivity" />
        <activity
            android:name=".ui.activity.MenuActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>